--- crates/openproxy-core/src/discovery_scheduler.rs
+++ crates/openproxy-core/src/discovery_scheduler.rs
@@ -431,10 +431,14 @@
                     }
                 }
             } else {
-                let w = db_pool.writer();
-                match accounts::decrypt_api_key(&w, acc.id, master_key.as_ref()) {
+                let result = {
+                    let w = db_pool.writer();
+                    accounts::decrypt_api_key(&w, acc.id, master_key.as_ref())
+                };
+
+                match result {
                     Ok(k) => (k, label),
                     Err(e) => {
                         tracing::warn!(
                             provider = %provider,
                             account_id = acc.id.0,
