/target
**/*.rs.bk
Cargo.lock.bak
.env
.env.*
*.env
data.db*
.openproxy/
/other_projects_examples
*.log
*.db
*.db*
*.pid
.worktrees/
.claude/
.codex/
.shannon/
.hermes/

# Internal specs (working docs, not for remote)
docs/specs/

# Temporary analysis & scratch text files
ci_result.txt
desc.txt
oauth_refresh.txt
ANALISIS_REFACTOR_DEDUPLICACION.md

# Playwright / Node artifacts generated by `npm install` and
# `playwright test`. The committed spec files live alongside,
# but `node_modules/`, `test-results/`, and `package-lock.json`
# are derived state and should not enter the repo.
node_modules/
test-results/
package-lock.json
*.rcgu.o


# runtime artifacts
data/
.reports/
