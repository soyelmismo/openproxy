/target
**/*.rs.bk
Cargo.lock.bak
.env
data.db*
.openproxy/
/other_projects_examples
*.log
*.db
*.env
*.pid
.worktrees/
# Playwright / Node artifacts generated by `npm install` and
# `playwright test`. The committed spec files live alongside,
# but `node_modules/`, `test-results/`, and `package-lock.json`
# are derived state and should not enter the repo.
node_modules/
test-results/
package-lock.json
*.db*
